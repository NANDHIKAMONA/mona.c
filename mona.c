#include<stdio.h>
#include<string.h>
int main()
{
    int a[10],i,c,b,n;
    printf("enter the string");
    scanf("%d",&a[i]);
    if(a[i]==0)
    {
        c++;
    }
    if(a[i]==1)
    {
        b++;
    }
    if(c>=0&&b<=1)
    {
        printf("yes");
    }
    else
    {
        printf("no");
    }
    return 0;
}
